fastapi==0.141.1
uvicorn[standard]==0.35.0
python-multipart==0.0.32
requests==2.33.1
PyMuPDF==1.28.2
python-docx==1.2.0
lxml==6.1.3
python-icap==0.2.0
prometheus-client==0.26.0
# Pillow 12.3.0 : dernière version stable au moment de l'écriture (PyPI),
# aucune CVE connue à cette version (vérifié via OSV.dev) — utilisée pour le
# décodage/caviardage/dépouillement de métadonnées des images PNG/JPEG.
Pillow==12.3.0
# pytesseract 0.3.13 : dernière version stable (PyPI), aucune CVE connue
# (vérifié via OSV.dev) — wrapper Python du binaire tesseract-ocr (voir
# Dockerfile pour le paquet système et la limite connue sur celui-ci).
pytesseract==0.3.13
pytest>=9.0.3,<10
