fastapi==0.141.1
uvicorn[standard]==0.35.0
python-multipart==0.0.32
requests==2.33.1
PyMuPDF==1.28.2
python-docx==1.2.0
lxml==6.1.3
python-icap==0.2.0
prometheus-client==0.26.0
# Pillow 12.3.0: latest stable version at time of writing (PyPI),
# no known CVE for this version (checked via OSV.dev) — used for
# decoding/redaction/metadata stripping of PNG/JPEG images.
Pillow==12.3.0
# pytesseract 0.3.13: latest stable version (PyPI), no known CVE
# (checked via OSV.dev) — Python wrapper around the tesseract-ocr binary (see
# Dockerfile for the system package and its known limitation).
pytesseract==0.3.13
pytest>=9.0.3,<10
